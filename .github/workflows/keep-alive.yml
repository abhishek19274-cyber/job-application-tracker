name: Keep Render App Awake

on:
  schedule:
    - cron: '*/12 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the app
        run: |
          curl -s https://job-application-tracker-i2h1.onrender.com/ -o /dev/null -w "Status: %{http_code}\n"
